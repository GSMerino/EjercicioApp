//import { usePost } from "../../hooks/usePost"



export const PostPage = () => {

    //const { data } = usePost()

    return(

        <section className="max-w-3xl max-h-[90vh] overflow-y-auto">
            post detail
        </section>
    )
}